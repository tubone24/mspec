# Checklist: spec grep/list サブコマンド

> Generated by mspec-checklist-auditor | Change: 2026-05-14-050811-spec-grep

## Delta Spec Coverage

### FR-011 — `mspec spec list-requirements`
- [x] MUST/SHALL 節あり: "capability ごとにグループ化して出力 MUST" — Delta Spec 行 7
- [x] Scenario「全 capability の Requirement が capability 単位でまとまって出力される」GIVEN/WHEN/THEN あり
- [x] Scenario「glob フィルタが正しく適用される」GIVEN/WHEN/THEN あり
- [x] design.md が `src/commands/spec-list-requirements.ts` に FR-011 を対応付け済み
- [ ] **[tasks]** E2E テストに glob 指定でゼロ件一致（no-match-glob）のエッジケースを追加する（Delta Spec Scenario には未記載、実装テストで補完）

### FR-012 — `mspec spec grep <fr-id>`
- [x] MUST 節あり: 横断検索・フォーマット検証・空結果 exit-0 を含む — Delta Spec 行 19–20
- [x] Scenario「SoT スペックの FR が正確に返される」GIVEN/WHEN/THEN あり
- [x] Scenario「Delta Spec の FR も検索対象に含まれる」GIVEN/WHEN/THEN あり
- [x] Scenario「不正な形式の ID はエラーになる」GIVEN/WHEN/THEN あり
- [x] design.md が `src/commands/spec-grep.ts` に FR-012 を対応付け済み
- [ ] **[tasks]** E2E テストで「空の結果 + exit 0」と「不正 ID + exit 1」を別テストケースとして区別すること
- [ ] **[tasks]** `/^FR-\d{1,4}$/i` は case-insensitive のため、`fr-001` 入力時の動作（受容/拒否）を E2E で明示すること（design での `i` フラグは lowercase 入力を受容する設計）
- [ ] **[tasks]** 5 桁の ID（`FR-12345`）がエラーになることを E2E テストで確認すること
- [ ] **[tasks]** `listChanges` がアクティブ change（現在の `2026-05-14-050811-spec-grep` 自身）を Delta Spec 検索に含めるか確認し、含める場合は `grep FR-011` が自身の Delta Spec をヒットすることを E2E テストで検証すること

### FR-013 — `mspec spec list-capabilities`
- [x] MUST 節あり: 昇順アルファベット順・1 行 1 capability・`specs/` 不在でエラー — Delta Spec 行 37–38
- [x] Scenario「capability 名が昇順で列挙される」GIVEN/WHEN/THEN あり
- [x] Scenario「`specs/` が存在しない場合のエラー」GIVEN/WHEN/THEN あり
- [x] design.md が `src/commands/spec-list-capabilities.ts` に FR-013 を対応付け済み
- [x] **[実装注意]** `collectSotSpecs` は `ENOENT` を silently `[]` で返す（spec-linter.ts:133–136）。FR-013 のエラー要件は `dirExists(specsDir)` を先行チェックして exit 1 でカバーする — design.md に記載済み
- [ ] **[tasks]** E2E テストで `specs/` ディレクトリが存在しないときに exit code が非ゼロになることを確認すること

### FR-014 — `--json` 出力モード（3 コマンド共通）
- [x] `command`・`results`・`meta` の 3 キー必須 — Delta Spec 行 50–51 + design.md JSON 例
- [x] Scenario「list-requirements の --json 出力が構造化されている」GIVEN/WHEN/THEN あり
- [x] Scenario「spec grep の --json 出力が構造化されている」GIVEN/WHEN/THEN あり
- [x] Scenario「list-capabilities の --json 出力が構造化されている」GIVEN/WHEN/THEN あり（本 checklist 生成後に Delta Spec へ追記済み）
- [ ] **[tasks]** `meta` フィールドにタイムスタンプや非決定的な値を含めないこと（決定論的出力 FR-006 の原則を新コマンドでも守る）

---

## Source-of-Truth 回帰リスク

### `collectSotSpecs` 拡張による FR-003 回帰リスク
- [x] `listCapabilityNames` は `collectSotSpecs` の戻り値を `map` するのみ — 既存関数の挙動を変更しない
- [x] `collectSotSpecs` の `archive` スキップロジック（spec-linter.ts:141）は `listCapabilityNames` にも自動継承される — `archive` ディレクトリが capability 一覧に現れないことを確認する
- [ ] **[tasks]** E2E テストで `specs/archive/spec.md` が存在してもリストに含まれないことを確認すること

### `spec` コマンドグループ拡張による FR-001〜FR-010 回帰リスク
- [x] Commander.js の `spec lint [glob]` は `[glob]` がオプション引数。新しい `.command('list-requirements [glob]')` 等は別サブコマンドとして登録されるため name 衝突なし
- [x] FR-008 の `{violations, summary}` エンベロープと FR-014 の `{command, results, meta}` エンベロープの意図的な不一致は proposal・design で明示的に受容済み — 統合計画なし
- [x] FR-010: `validateCommand` は `specLintCommand` / `lintSotSpecs` を直接呼ぶ。新コマンドを `spec` グループに追加してもこの経路に影響しない

### FR-006 決定論的出力の新コマンドへの適用
- [ ] **[tasks]** `list-requirements` の capability 出力順が `collectSotSpecs` の昇順ソート（アルファベット順）を継承することを E2E テストで確認すること
- [ ] **[tasks]** `grep` の結果順をファイルパス順 → ブロック開始行順で固定し、同一入力で同一出力が得られることをテストすること

---

## Constitution

### Principle I — ステップ独立性
- [x] 3 コマンドはすべて読み取り専用。`specs/` および `changes/` への書き込みなし
- [ ] **[tasks]** 実装後に `specs/` / `changes/` に副作用がないことを E2E テストのティアダウンで確認すること（`afterEach` で tmp ディレクトリ以外のパスが変更されていないこと）

### Principle II — 決定論的マージ
- [x] archive 対象は Delta Spec (`changes/.../specs/cli-spec-lint/spec.md`) のみ。コマンド実装ファイルは archive 非対象

### Principle III — 質問駆動の要件確定
- [x] research の Open Choice（`list-capabilities` スコープ）が設計フェーズで確定済み
- [x] 本 checklist で新たな未確定事項は発生していない

### Principle IV — 双方向アンカー
- [ ] **[tasks]** `tests/e2e/spec-grep.e2e.test.ts` の先頭に `@mspec-delta 2026-05-14-050811-spec-grep/specs/cli-spec-lint/spec.md` + `FR-011, FR-012, FR-013, FR-014` アンカーを付与すること
- [ ] **[tasks]** `src/commands/spec-list-requirements.ts`・`spec-grep.ts`・`spec-list-capabilities.ts` の各ファイル先頭にも同アンカーを付与すること
- [ ] **[tasks]** 実装後に `mspec anchor check` が 0 エラーで通ることを確認すること

### Principle V — 強制ステップと拡張ステップの分離
- [x] `workflow.yaml` は変更しない。`removable: false` ステップ（new・proposal・delta・tasks・implement・archive）は変更しない

### 追加制約
- [x] `--json` 出力は stdout のみ。エラーは stderr。終了コードは FR ごとに仕様化済み（FR-012: 不正 ID → 1, 未発見 → 0; FR-013: `specs/` 不在 → 1）
- [ ] **[tasks]** `meta` フィールドに非決定的な値（timestamp, uuid 等）を含めないことを実装時に徹底すること
