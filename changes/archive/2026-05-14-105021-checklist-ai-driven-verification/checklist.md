# Checklist: checklist AI-driven verification

> Generated by mspec-checklist-auditor | Change: 2026-05-14-105021-checklist-ai-driven-verification

## Delta Spec Coverage

### FR-011 — checklist-auditor annotates each item with verify metadata

- [x] Scenario「AI-verifiable item receives FR reference annotation」が design.md の受け入れ基準（Decisions テーブル行「アノテーション形式」）に対応付けられている
- [x] Scenario「AI-verifiable item receives FR reference annotation」— THEN 句「末尾に `<!-- verify: fr-011 -->` が付与される」が `mspec-checklist-auditor.md` の Constraints 節（runtime ファイル）に手順として記述されている
- [x] Scenario「AI-verifiable item receives FR reference annotation」— AND 句「1 項目に付与される `verify:` アノテーションは 1 つである」が auditor 実装の一意付与ルールとして明文化されている
- [x] Scenario「Human-review item receives human annotation」— THEN 句「`<!-- verify: human -->` が付与される」が `mspec-checklist-auditor.md` の Constraints 節に手順として記述されている
- [x] design.md が FR-011 のアノテーション形式例（`<!-- verify: fr-NNN -->` / `<!-- verify: human -->`）を具体的に示している（checklist.md アノテーション形式節参照）

### FR-012 — implement skill auto-checks checklist items when task reaches GREEN

- [x] Scenario「Test suite goes GREEN, corresponding checklist item is auto-checked」— THEN 句「`<!-- verify: fr-011 -->` 付き `- [ ]` が `- [x]` に更新される」が `mspec-implement/SKILL.md` の Step 3 直後の手順として記述されている
- [x] Scenario「Test suite goes GREEN, corresponding checklist item is auto-checked」— AND 句「他の `verify:` アノテーションは変更されない」が冪等性ルールとして明文化されている
- [x] Scenario「No matching verify annotation — checklist unchanged」— `tasks.md` アンカーに `FR-013` が記録されているが `checklist.md` に `<!-- verify: fr-013 -->` 項目が存在しない場合に `checklist.md` が変更されないことが手順に明記されている
- [x] design.md の「implement スキルの自動チェックロジック（FR-012）」節に記載の逆引き手順（task-anchor 読み取り → 走査 → 置換 → 冪等）が `mspec-implement/SKILL.md` に反映されている

### FR-013 — implement step reports unchecked items after all tasks complete

- [x] Scenario「Unchecked human items reported at end of implement」— THEN 句「`verify: human` 未チェック項目一覧をユーザーに提示し、人間レビューを求めブロックする」が `mspec-implement/SKILL.md` の全タスク完了後ステップに記述されている
- [x] Scenario「Unchecked fr-annotated items trigger gap warning」— THEN 句「対象 FR 番号とギャップ説明を報告し、ユーザーの確認を待つ（警告 + ブロック）」が `mspec-implement/SKILL.md` に記述されている
- [x] Scenario「All items checked — implementation declared complete」— THEN 句「未チェック項目の報告なしに実装完了を宣言する」が `mspec-implement/SKILL.md` に記述されている
- [x] design.md の「未チェック項目報告ロジック（FR-013）」節に記載の 3 分岐（human / fr-NNN gap / ゼロ件）がすべて `mspec-implement/SKILL.md` に対応している

### FR-014 — Runtime skill files and CLI template files are kept in sync

- [x] Scenario「Template and runtime skill contain identical verify procedure」— `.claude/agents/mspec-checklist-auditor.md`（runtime）と `packages/cli/templates/claude/agents/mspec-checklist-auditor.md`（template）の `verify:` アノテーション手順が内容一致している
- [x] `.claude/skills/mspec-implement/SKILL.md`（runtime）と `packages/cli/templates/claude/skills/mspec-implement/SKILL.md`（template）の自動チェックロジックおよび未チェック報告ロジックが内容一致している
- [x] 4 ファイルすべてに design.md「アンカーパターン」節に示した `@mspec-delta` アンカーブロックが付与されている
- [x] `mspec anchor check` が 4 ファイルに対して 0 エラーで通ることを実装後に確認する

---

## Source-of-Truth Regression

### claude-integration SoT — FR-002: One SKILL.md per workflow step

- [x] `mspec-implement/SKILL.md` の改変後も YAML frontmatter（`name`, `description`, `when_to_use`）と `## Procedure` 見出しが維持されている（FR-002 の構造要件）

### claude-integration SoT — FR-003: SKILL.md procedure starts with `mspec status --json`

- [x] `mspec-implement/SKILL.md` の Step 3 直後に追加された自動チェック手順が、Step 1 の `mspec status --change <name> --json` より前に移動していないことを確認する（FR-003 の順序要件）

### claude-integration SoT — FR-004: Subagent files under `.claude/agents/`

- [x] `mspec-checklist-auditor.md` の改変後も `.claude/agents/mspec-checklist-auditor.md` としてファイルが存在し続けることを確認する（FR-004 の存在要件）

### claude-integration SoT — FR-006: Role boundary between Claude, subagent, and CLI

- [x] `mspec-checklist-auditor.md` に追加される `verify:` アノテーション付与手順が、CLI ロジックの再実装（`mspec anchor check` 等の処理のインライン化）を含まないことを確認する（FR-006 の役割境界）
- [x] `mspec-implement/SKILL.md` の自動チェックロジック（checklist.md の行走査・置換）は Skill 側テキスト処理として許容範囲内であり、CLI に委譲すべき構造検証を代替していないことを確認する

### cli-anchor SoT — FR-001/FR-003: Markdown ファイルへのアンカー形式

- [x] 4 つの Markdown ファイルに付与する `@mspec-delta` アンカーが HTML コメント形式（`<!-- @mspec-delta ... -->`）で記述され、cli-anchor FR-001 の 3 行フォーマットに準拠していることを確認する
- [x] cli-anchor FR-003 のコメントプレフィックス剥ぎ取り対象に HTML コメント形式（`<!-- ... -->`）が含まれることを確認する。含まれない場合は `mspec anchor check` が Markdown ファイルのアンカーを認識できないリスクがある（cli-anchor SoT に HTML コメント形式の Scenario が存在しないため回帰リスクあり）— **スコープ外として承認。`blankOutHtmlComments` による制限は CLI TypeScript Non-Goal として tasks.md T001 に記録済み。将来の CLI 拡張タスクとして管理する**

### cli-init SoT — FR-007（テンプレート配布）との整合

- [x] `packages/cli/templates/claude/agents/mspec-checklist-auditor.md` および `packages/cli/templates/claude/skills/mspec-implement/SKILL.md` の改変が、`mspec init --tools claude` 実行時のファイル配布経路（cli-init）に自動的に反映されることを確認する（cli-init SoT に template 配布に関する Requirement が明示されていないため、配布ロジックへの追加対応が不要であることを確認する）

### cli-workflow-engine SoT — workflow.yaml 不変

- [x] 今回の変更で `.mspec/workflow.yaml` の `implement` ステップ定義または `checklist` ステップ定義が変更されていないことを確認する（cli-workflow-engine FR-002 の mandatory steps 不変要件）

---

## Constitution

### Principle I — ステップ独立性

- [x] `mspec-implement/SKILL.md` への自動チェック追加後も、checklist ステップ（auditor が `checklist.md` を生成）と implement ステップ（`verify:` アノテーションを読んで更新）が互いに独立して機能することを確認する。implement は `checklist.md` の静的メタデータを読むのみで auditor の生成ロジックに依存しない
- [x] `mspec-tasks` SKILL.md が今回の変更で改変されていないことを確認する（FR 参照方式採用により tasks ステップへの依存が増えていない）

### Principle II — 決定論的マージ

- [x] 変更対象が Skill / Agent の Markdown ファイル 4 本のみであり、CLI の archive / merge ロジックに触れていないことを確認する
- [x] `mspec archive` 実行時のマージ対象が `specs/claude-integration/spec.md` のみであることを確認する（checklist.md や SKILL.md は archive 非対象）

### Principle III — 質問駆動の要件確定

- [x] アノテーション形式（`fr-NNN` 参照方式 vs task-ID 方式）の選択根拠が research.md または design.md に追跡可能な形で記録されていることを確認する
- [x] 報告トーン（警告 + ブロック）の採用根拠が design.md の Decisions テーブルに記録されていることを確認する

### Principle IV — 双方向アンカー

- [x] 実装対象の 4 ファイルすべてに `@mspec-delta 2026-05-14-105021-checklist-ai-driven-verification/specs/claude-integration/spec.md` を含む 3 行アンカーブロックが付与されていることを確認する
- [x] `mspec anchor check` が 0 エラーで通ることを実装後に確認する

### Principle V — 強制ステップと拡張ステップの分離

- [x] `workflow.yaml` が変更されていないことを確認する
- [x] `mspec-checklist` スキル本体の呼び出し手順（`mspec continue` → subagent 起動プロトコル）が変更されていないことを確認する
- [x] implement への自動チェック追加が既存 `implement` ステップの手順拡張であり、新ステップの追加や mandatory steps の変更を伴わないことを確認する
