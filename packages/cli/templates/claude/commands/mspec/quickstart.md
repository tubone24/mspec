---
description: Write quickstart.md for the change
---

You are in the **quickstart** step of the mspec workflow.

## Procedure

1. Run `mspec status --change <change-dir> --json` and read `design.md`.
2. Write `quickstart.md` using the template: Prerequisites, Setup, Try it (Golden Path), Verify, Troubleshooting.
3. Run `mspec validate --change <change-dir>`.
4. `block: false` — auto-continue to checklist.
